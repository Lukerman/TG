# Telegram Bot Core (REQUIRED)
python-telegram-bot>=20.3
httpx>=0.27.0

# MongoDB Async Driver (REQUIRED)
motor>=3.3.0
pymongo>=4.6.0
pymongo[srv]>=4.6.0
dnspython>=2.0.0

# Environment Variables (REQUIRED)
python-dotenv>=1.0.0

# File Type Detection (for attachments) (REQUIRED)
python-magic>=0.4.27

# System Dependencies Note:
# For python-magic to work, you may need to install:
# Ubuntu/Debian: sudo apt-get install libmagic1
# CentOS/RHEL: sudo yum install file-devel
# macOS: brew install libmagic

# Additional Dependencies (auto-installed with above)
certifi>=2023.0.0
typing-extensions>=4.0.0
anyio>=4.0.0
httpcore>=1.0.0
h11>=0.16.0
sniffio>=1.1.0
idna>=3.4.0

# Development and Testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0

# For virtual environment setup (optional)
virtualenv>=20.0.0